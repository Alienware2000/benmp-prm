# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
BENMP_DATA_PROVIDER=mock
BENMP_MESSAGING_PROVIDER=mock

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# AI SDK / model gateway
AI_GATEWAY_API_KEY=
BENMP_DEFAULT_MODEL=

# Payments: none — money enters via CSV import, which needs no credentials (Decision 0007).

# Communication providers
# Training wheels: when set (comma/space-separated phone numbers), real sends only reach
# these numbers. Delete the variable when leadership approves full sends.
BENMP_SEND_ALLOWLIST=
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_MESSAGING_SERVICE_SID=
TWILIO_SMS_FROM=
TWILIO_WHATSAPP_SENDER=

# Email
RESEND_API_KEY=

# POC console gate (set POC_PASSWORD in any deployed env — /poc shows real partner data)
POC_USER=benmp
POC_PASSWORD=
